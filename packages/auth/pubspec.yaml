name: auth
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: 2.0.0
  provider: ^4.0.0
  shared_preferences: 2.0.8
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
